{
  "name": "client",
  "version": "0.1.0",
  "scripts": {
    "compile": "bsb -make-world",
    "start": "bsb -make-world -w -ws _ ",
    "clean": "bsb -clean-world && rm -rf bundleOutput/*.js",
    "dev-server": "moduleserve ./ --port 8000 --spa",
    "build": "yarn clean && yarn compile && webpack",
    "prod-server": "yarn build && serve -s ./bundleOutput",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "BuckleScript",
    "ReasonReact",
    "reason-react"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@glennsl/bs-json": "^5.0.2",
    "bs-css-emotion": "^1.2.0",
    "bs-fetch": "^0.5.2",
    "bs-json": "^1.0.1",
    "bs-let": "^0.1.16",
    "react": "^16.8.1",
    "react-dom": "^16.8.1",
    "reason-react": ">=0.7.1"
  },
  "devDependencies": {
    "bs-platform": "^7.3.2",
    "moduleserve": "^0.9.0",
    "serve": "^11.3.1",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.11"
  }
}
